<Window x:Class="InSight_Manager.View.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Loading..." Height="300" Width="500"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        Topmost="True">

    <Border Background="#1E1E1E" CornerRadius="15" BorderBrush="#333333" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.5" Color="Black"/>
        </Border.Effect>

        <Grid>
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="VISION SYSTEM" Foreground="White" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center"/>
                <TextBlock Text="In-Sight Inspection Manager" Foreground="#AAAAAA" FontSize="14" HorizontalAlignment="Center" Margin="0,5,0,30"/>

                <ProgressBar IsIndeterminate="True" Width="300" Height="4" BorderThickness="0" Background="#333333" Foreground="#007ACC">
                    <ProgressBar.Resources>
                        <Style TargetType="ProgressBar">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ProgressBar">
                                        <Grid>
                                            <Border Name="PART_Track" Background="{TemplateBinding Background}" CornerRadius="2"/>
                                            <Border Name="PART_Indicator" Background="{TemplateBinding Foreground}" CornerRadius="2" HorizontalAlignment="Left"/>
                                        </Grid>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ProgressBar.Resources>
                </ProgressBar>

                <TextBlock x:Name="StatusText" Text="Initializing Sensor Connection..."
                           Foreground="#888888" FontSize="12" HorizontalAlignment="Center" Margin="0,15,0,0"/>
            </StackPanel>

            <TextBlock Text="v1.0.0" VerticalAlignment="Bottom" HorizontalAlignment="Right" Foreground="#444444" Margin="15" FontSize="10"/>
        </Grid>
    </Border>
</Window>